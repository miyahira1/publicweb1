{
  "id": "claude-code-overview",
  "title": "Claude Code Overview",
  "content": "## What Is Claude Code?\n\nClaude Code is Anthropic's official CLI and agentic coding tool. It gives Claude direct access to your codebase — reading files, running shell commands, editing code, running tests, and making git commits — all driven by natural language instructions.\n\nAvailable as:\n- **CLI** (`claude` command in terminal)\n- **Desktop app** (Mac and Windows)\n- **Web app** (claude.ai/code)\n- **IDE extensions** (VS Code, JetBrains)\n\n---\n\n## Remote Sessions (Claude Code on the Web)\n\nWhen you use Claude Code via the web or mobile apps, your session runs in an **isolated cloud container** — not on your device. Key implications:\n\n- **Fresh clone:** The repo is cloned when the container starts. Any changes not committed and pushed are lost when the session ends.\n- **Ephemeral state:** Installed packages, running processes, and environment variables don't persist between sessions unless they're in the repo or in startup hooks.\n- **Network access:** Governed by the environment's network policy, set when the environment is created. Can be open (outbound internet), restricted, or offline.\n- **Inactivity timeout:** Containers are reclaimed after a period of inactivity. Long-running processes need to complete before the session ends.\n\n**Implication for this playground:** Every tool, game, and wiki page is static HTML/JSON. The agent commits and pushes; GitHub Pages serves the result. Nothing needs to persist in the container.\n\n---\n\n## Hooks\n\nHooks are shell scripts that run automatically in response to events in the Claude Code session. Defined in `.claude/settings.json`:\n\n```json\n{\n  \"hooks\": {\n    \"SessionStart\": [\n      {\n        \"hooks\": [{\n          \"type\": \"command\",\n          \"command\": \"$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh\"\n        }]\n      }\n    ]\n  }\n}\n```\n\n**Common hook events:**\n- `SessionStart` — runs when a session starts (install dependencies, set env vars)\n- `PreToolCall` — runs before Claude calls a tool (logging, safety checks)\n- `PostToolCall` — runs after a tool call completes\n- `Stop` — runs when Claude finishes its turn\n\n**Async hooks:** Add `{\"async\": true}` to a hook's stdout to run it in the background, reducing session startup latency.\n\n---\n\n## Slash Commands\n\nBuilt-in slash commands provide quick access to common operations:\n\n| Command | Effect |\n|---|---|\n| `/help` | Show available commands |\n| `/clear` | Clear conversation context |\n| `/compact` | Summarize and compress context |\n| `/config` | Open settings |\n| `/fast` | Toggle Fast mode (Opus with faster output) |\n| `/memory` | Edit Claude's persistent memory |\n| `/review` | Launch a code review |\n\nCustom slash commands can be added as markdown files in `.claude/commands/`.\n\n---\n\n## Multi-Agent Features\n\nClaude Code supports spawning subagents for parallel or specialized work:\n\n- **Agent tool:** One Claude instance can launch another to handle a subtask, then incorporate the result\n- **Parallel agents:** Multiple independent subagents can run concurrently, with results merged by the coordinator\n- **Specialized agents:** Agent definitions (in `.claude/agents/`) give subagents a custom system prompt, tools, and model selection\n\nThis is how complex tasks like \"review this entire codebase and fix all type errors\" are handled — a coordinator breaks the work into chunks, subagents tackle each file or module, and results are assembled.\n\n---\n\n## Scheduled Tasks\n\nClaude Code supports scheduled prompts — a prompt that runs automatically on a cron-like schedule in a given repo's context. The agent has full access to read/write files, run commands, and push changes.\n\n**Use case for this playground:** The `wikis/AGENT_PROMPT.md` file contains the exact prompt used to run the wiki maintenance agent. Set it as a scheduled task (daily or weekly) and the wikis will update automatically.\n\n---\n\n## Settings Files\n\n| File | Scope |\n|---|---|\n| `.claude/settings.json` | Project-level (committed to repo, shared with team) |\n| `.claude/settings.local.json` | Project-level, local only (gitignored) |\n| `~/.claude/settings.json` | User-level (applies to all projects) |\n\nSettings control: model selection, permission allowlists, hook definitions, env vars, and behavioral preferences.\n\n---\n\n## Permission Model\n\nClaude Code prompts for permission before taking potentially risky actions (writing files, running commands, making network requests). Permissions can be pre-approved in settings to reduce prompts:\n\n```json\n{\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(git *)\",\n      \"Bash(npm run *)\"\n    ]\n  }\n}\n```\n\nWildcard patterns (`*`) grant permission for any argument matching the prefix.\n\n---\n\n## 2026 Feature Timeline\n\nClaude Code ships weekly updates (tracked at `code.claude.com/docs/en/whats-new`). The highlights since mid-2025:\n\n### Auto Mode\nEntered research preview in March 2026: a classifier evaluates each action and lets safe ones run without a permission prompt while blocking risky ones — a middle ground between approving everything and `--dangerously-skip-permissions`. It expanded to the Pro plan (May 2026), then to Opus 4.7/4.8 on Amazon Bedrock, Google Cloud's Agent Platform, and Microsoft Foundry (June 2026). Hard deny rules now block actions unconditionally regardless of allow exceptions, and auto mode blocks destructive git commands you didn't ask to run.\n\n### Agent Orchestration at Scale\n- **Dynamic workflows** (May 2026): Claude writes a script that orchestrates dozens to hundreds of subagents for problems too large for a single agent tree.\n- **Nested sub-agents** (June 2026): background subagent chains can spawn their own sub-agents, capped at five levels deep.\n- **Agent view** (`claude agents`, May 2026): one screen showing every session — what's running, blocked on you, or done.\n- Background subagents now surface permission prompts in the main session (June 2026) instead of silently auto-denying.\n\n### Effort Control\nOpus 4.7 (April 2026) introduced an `xhigh` effort level, recommended for most coding work, adjustable via an interactive `/effort` slider or the `effort.level` hook variable / `$CLAUDE_EFFORT` env var.\n\n### Cloud & Scheduling\n- **Routines** (April 2026): templated cloud agents fire on a schedule, GitHub event, or API call — the mechanism behind this playground's own wiki-maintenance agent.\n- **Ultraplan** (April 2026, early preview): draft a plan in the cloud, review and comment on it in a web editor, then run it remotely or pull it back local.\n- **`/ultrareview`** (April-May 2026): a fleet of bug-hunting agents runs in the cloud and reports back; `claude ultrareview` brings the same review to CI and scripts.\n- Mobile push notifications ping your phone when a long task finishes or Claude needs input.\n\n### New Commands & UX\n- `/goal` keeps Claude working across turns until a completion condition holds.\n- `/usage` breaks down what's driving plan limits by skill, subagent, plugin, and MCP server.\n- `/cd` moves a session to a new working directory mid-conversation without rebuilding the prompt cache.\n- `/rewind` can resume a conversation from before `/clear` was run; the Rewind menu can also compress earlier context with \"Summarize up to here.\"\n- `fallbackModel` configures up to three fallback models tried in order.\n- The CLI moved to native binaries in April 2026 (no Node.js runtime required), and gained a native PowerShell shell tool so Windows no longer needs Git Bash.\n\n### MCP & Plugins\n- `claude mcp login` / `claude mcp logout` (June 2026) authenticate or clear credentials for a configured MCP server from the shell instead of the `/mcp` menu.\n- Plugins can load from `.zip` archives and URLs via `--plugin-dir` / `--plugin-url`, and `/plugin list` prints installed plugins inline.\n- Deny/ask permission rules can match tool parameters, e.g. `Agent(model:opus)`.\n\n### Artifacts in Claude Code\nIn beta on Team and Enterprise plans (June 2026): a session's output can become a live, shareable page on claude.ai that updates in place as the session keeps working — distinct from the original claude.ai Artifacts feature for generated code/documents.",
  "tags": ["claude-code", "cli", "agentic", "remote", "hooks", "slash-commands", "scheduled-tasks", "auto-mode", "dynamic-workflows", "2026-updates"],
  "sources": [
    "https://docs.anthropic.com/en/docs/claude-code",
    "https://code.claude.com/docs",
    "https://code.claude.com/docs/en/whats-new"
  ],
  "createdAt": "2025-05-21",
  "updatedAt": "2026-07-05",
  "relatedPages": ["tool-use-and-agents", "claude-model-history"]
}
